from distutils.core import setup
setup(
        name='',
        version='',
        packages=['routing_engine', 'test', 'apis'],
        license='',
        long_description=open('README.md').read(),
        )
